#include <cstdio>
#include <cstring>
#include <stack>
using namespace std;

stack<char> s;
string p;

int main()
{
    scanf("%s", &p);
    int len = strlen(p);
    for (int i = 0; i < len;i++){
        if(c[i]=='('||c[i]=='[')
            s.push(c[i]);
        
    }
    return 0;
}