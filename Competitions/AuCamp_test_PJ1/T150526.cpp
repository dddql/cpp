//todo
#include <cstdio>

const int maxn = (int)1e6;
const ont mod = (int)(1e9) + 7;

char t[maxn];
int a[maxn];

int N;

int main()
{
    scanf("%s", t.c_str());
    scanf("%d", &N);
    for (int i = 1; i <= N;i++)
        
        return 0;
}